# トラブルシューティング - AI FX市場監視Bot (メキシコペソ版)

このドキュメントでは、実際の開発・デプロイ中に発生した問題とその解決方法を記録しています。

**最終更新**: 2026-01-04

---

## 目次

1. [Python バージョン互換性エラー](#1-python-バージョン互換性エラー)
2. [venv フォルダ構成の問題 (Windows vs Linux)](#2-venv-フォルダ構成の問題-windows-vs-linux)
3. [Cloud Scheduler 認証エラー - OIDC vs OAuth](#3-cloud-scheduler-認証エラー---oidc-vs-oauth)
4. [Cloud Scheduler 権限エラー (PERMISSION_DENIED)](#4-cloud-scheduler-権限エラー-permission_denied)
5. [環境変数の設定エラー (PowerShell)](#5-環境変数の設定エラー-powershell)
6. [Cloud Run Job の削除と復旧](#6-cloud-run-job-の削除と復旧)
7. [複数プロジェクトの混在](#7-複数プロジェクトの混在)

---

## 1. Python バージョン互換性エラー

### 発生した問題

Cloud Run Jobsの実行時に以下のエラーが発生：

```
TypeError: unsupported operand type(s) for |: 'types.GenericAlias' and 'types.GenericAlias'
File "/usr/local/lib/python3.9/site-packages/yfinance/calendars.py", line 33
    def __init__(self, operator: str, operand: list[Any] | list["CalendarQuery"]):
```

### 原因

- Dockerfileで `FROM python:3.9-slim` を指定していた
- yfinanceライブラリがPython 3.10以上の型ヒント構文 (`list[Any] | list["Query"]`) を使用している
- Python 3.9ではこの `|` (Union) 演算子がサポートされていない

### 解決方法

Dockerfileのベースイメージを変更：

```dockerfile
# 変更前
FROM python:3.9-slim

# 変更後
FROM python:3.11-slim
```

変更後、再デプロイを実行：

```powershell
gcloud run jobs deploy fx-mxn-watcher `
  --source . `
  --region asia-northeast1
```

### 予防策

- requirements.txtに依存ライブラリを記載する際は、必要なPythonバージョンも確認する
- yfinanceの場合、公式ドキュメントやGitHubで最小バージョン要件を確認すべき
- **今後の推奨**: Dockerfileには `python:3.11-slim` 以上を指定

---

## 2. venv フォルダ構成の問題 (Windows vs Linux)

### 発生した問題

仮想環境を作成したはずなのに、`.\venv\Scripts\Activate.ps1` が見つからないエラー：

```
.\venv\Scripts\Activate.ps1 : 用語 '.\venv\Scripts\Activate.ps1' は、コマンドレット、関数、スクリプト ファイル、または操作可能なプログラムの名前として認識されません。
```

### 原因

- WSL (Windows Subsystem for Linux) 環境で `python -m venv venv` を実行してしまった
- LinuxとWindowsではvenvのフォルダ構成が異なる：
  - **Linux**: `venv/bin/activate`
  - **Windows**: `venv\Scripts\Activate.ps1`
- WSLで作成したvenvはLinux形式になるため、Windows PowerShellから起動できない

### 解決方法

#### Step 1: 既存のvenvを削除

```powershell
Remove-Item -Recurse -Force venv
```

#### Step 2: Windows PowerShellで再作成

```powershell
# 必ずWindows PowerShellで実行
python -m venv venv
.\venv\Scripts\Activate.ps1
```

#### 確認方法

```powershell
# Scriptsフォルダが存在することを確認
Get-ChildItem .\venv\Scripts\
```

### 予防策

- **必ずWindows PowerShellで仮想環境を作成する**
- WSL環境とWindows環境を混在させない
- フォルダ構成を確認してから作業を進める

---

## 3. Cloud Scheduler 認証エラー - OIDC vs OAuth

### 発生した問題

Cloud Schedulerが毎時実行を試みるが、すべて失敗する。ログを確認すると以下のエラーが繰り返し発生：

```
status: UNAUTHENTICATED
httpRequest:
  status: 401
debugInfo: URL_ERROR-ERROR_AUTHENTICATION. Original HTTP response code number = 401
```

### 原因

Cloud Schedulerの作成時に **OIDC認証**を使用していたが、Cloud Run **Jobs** API は **OAuth認証**を必要とする。

**重要な違い**:

| 項目 | OIDC (OpenID Connect) | OAuth 2.0 |
|:---|:---|:---|
| **用途** | ID認証・ユーザー身元確認 | API認可・リソースアクセス権限 |
| **適用先** | Cloud Run **Services** (常時起動型) | Cloud Run **Jobs** (バッチ実行型) |
| **トークン** | IDトークン（誰であるかを証明） | アクセストークン（何ができるかを証明） |
| **gcloud パラメータ** | `--oidc-service-account-email` | `--oauth-service-account-email` |

Cloud Run **Jobs** は REST API エンドポイントのため、API呼び出し認可用の **OAuth** が必要です。

### 解決方法

#### Step 1: 現在のCloud Schedulerを削除

```powershell
gcloud scheduler jobs delete fx-mxn-watcher-schedule --location=asia-northeast1
```

#### Step 2: OAuth認証で再作成

```powershell
$PROJECT_ID = gcloud config get-value project

gcloud scheduler jobs create http fx-mxn-watcher-schedule `
  --location=asia-northeast1 `
  --schedule="0 * * * *" `
  --time-zone="Asia/Tokyo" `
  --uri="https://asia-northeast1-run.googleapis.com/apis/run.googleapis.com/v1/namespaces/${PROJECT_ID}/jobs/fx-mxn-watcher:run" `
  --http-method=POST `
  --oauth-service-account-email="cloud-scheduler-runner@${PROJECT_ID}.iam.gserviceaccount.com"
```

**重要**: `--oauth-service-account-email` を使用（`--oidc` ではない）

#### Step 3: 動作確認

```powershell
# 手動トリガー
gcloud scheduler jobs run fx-mxn-watcher-schedule --location=asia-northeast1

# 実行履歴を確認
gcloud run jobs executions list --job=fx-mxn-watcher --region=asia-northeast1 --limit=10
```

`RUN BY` 列に `cloud-scheduler-runner@...` が表示されれば成功です。

### 確認方法

作成したCloud Schedulerの設定を確認：

```powershell
gcloud scheduler jobs describe fx-mxn-watcher-schedule --location=asia-northeast1
```

以下のように `oauthToken` が表示されていればOK：

```yaml
httpTarget:
  oauthToken:                                    # ← OAuthトークン
    scope: https://www.googleapis.com/auth/cloud-platform
    serviceAccountEmail: cloud-scheduler-runner@...
```

もし `oidcToken` が表示されている場合は、削除して再作成が必要です。

### 予防策

- Cloud Run **Services** → OIDC認証
- Cloud Run **Jobs** → **OAuth認証** ⭐
- 手順書のコマンドを必ず確認して実行する
- Web Consoleで作成する場合は「OAuth トークンを追加」を選択

---

## 4. Cloud Scheduler 権限エラー (PERMISSION_DENIED)

### 発生した問題

Cloud Schedulerジョブを実行すると以下のエラーが発生：

```
Error: PERMISSION_DENIED (403)
Failed to trigger execution of job fx-mxn-watcher
```

### 原因

- Cloud SchedulerがCloud Run Jobsを実行する権限（`run.invoker`）を持っていなかった
- 従来使用していた `Compute Engine default service account` が新しいプロジェクトでは存在しなかった
- サービスアカウントの設定が不足していた

### 解決方法

#### Step 1: 専用サービスアカウントの作成

```powershell
gcloud iam service-accounts create cloud-scheduler-runner `
  --display-name="Cloud Scheduler Runner"
```

#### Step 2: プロジェクトIDの取得

```powershell
$PROJECT_ID = gcloud config get-value project
```

#### Step 3: 権限の付与

```powershell
gcloud projects add-iam-policy-binding $PROJECT_ID `
  --member="serviceAccount:cloud-scheduler-runner@${PROJECT_ID}.iam.gserviceaccount.com" `
  --role="roles/run.invoker"
```

#### Step 4: Cloud Schedulerジョブの作成（サービスアカウント指定）

```powershell
gcloud scheduler jobs create http fx-mxn-watcher-schedule `
  --location=asia-northeast1 `
  --schedule="0 * * * *" `
  --uri="https://asia-northeast1-run.googleapis.com/apis/run.googleapis.com/v1/namespaces/${PROJECT_ID}/jobs/fx-mxn-watcher:run" `
  --http-method=POST `
  --oauth-service-account-email="cloud-scheduler-runner@${PROJECT_ID}.iam.gserviceaccount.com"
```

#### 動作確認

```powershell
gcloud scheduler jobs run fx-mxn-watcher-schedule --location=asia-northeast1
```

### 予防策

- Cloud Schedulerを設定する際は、必ず適切な権限を持つサービスアカウントを指定する
- `Compute Engine default service account` に依存せず、専用アカウントを作成する
- IAMの権限設定を確認してからジョブを作成する

---

## 5. 環境変数の設定エラー (PowerShell)

### 発生した問題

環境変数を設定しようとしたら、コマンドが認識されないエラー：

```
env:OPENAI_API_KEY : 用語 'env:OPENAI_API_KEY' は、コマンドレット、関数、スクリプト ファイル、または操作可能なプログラムの名前として認識されません。
```

### 原因

PowerShellで環境変数を設定する際、`$` 記号を忘れていた：

```powershell
# 誤り
env:OPENAI_API_KEY="sk-..."

# 正しい
$env:OPENAI_API_KEY="sk-..."
```

### 解決方法

正しい構文で環境変数を設定：

```powershell
$env:OPENAI_API_KEY="sk-proj-..."
```

### 予防策

- PowerShellでは環境変数の読み書きに必ず `$env:` を使用する
- Bashとの構文の違いに注意する（Bashでは `export OPENAI_API_KEY="..."`）

---

## 6. Cloud Run Job の削除と復旧

### 発生した問題

誤ってCloud Run Jobを削除してしまった。

### 原因

- Google Cloud Consoleで操作中に誤ってジョブを削除
- バックアップやスナップショットがない状態での削除

### 解決方法

#### Step 1: 現状確認

```powershell
# プロジェクト設定
gcloud config set project fx-mxn-watcher-bot

# Cloud Run Jobの確認
gcloud run jobs list --region=asia-northeast1

# Cloud Schedulerの確認
gcloud scheduler jobs list --location=asia-northeast1
```

#### Step 2: Cloud Run Jobの再作成

```powershell
# プロジェクトフォルダに移動
cd C:\path\to\ai-market-watchdog-mxn

# 再デプロイ
gcloud run jobs deploy fx-mxn-watcher `
  --source . `
  --region asia-northeast1

# 環境変数を再設定
gcloud run jobs update fx-mxn-watcher `
  --set-env-vars OPENAI_API_KEY="sk-proj-..." `
  --region asia-northeast1

# 動作確認
gcloud run jobs execute fx-mxn-watcher --region=asia-northeast1
```

#### Step 3: Cloud Schedulerの再作成（削除されていた場合）

```powershell
$PROJECT_ID = gcloud config get-value project

gcloud scheduler jobs create http fx-mxn-watcher-schedule `
  --location=asia-northeast1 `
  --schedule="0 * * * *" `
  --uri="https://asia-northeast1-run.googleapis.com/apis/run.googleapis.com/v1/namespaces/${PROJECT_ID}/jobs/fx-mxn-watcher:run" `
  --http-method=POST `
  --oauth-service-account-email="cloud-scheduler-runner@${PROJECT_ID}.iam.gserviceaccount.com"
```

#### 詳細な復旧手順

詳細は `recreation_commands.md` を参照してください。

### 予防策

- 削除操作の前に必ず確認する
- 重要なリソースには削除保護を設定する
- Infrastructure as Code (IaC) ツール（Terraform等）で設定を管理し、コードとして保存しておく
- 定期的にバックアップや設定のスナップショットを取る

---

## 7. 複数プロジェクトの混在

### 発生した問題

Cloud Schedulerのコンソールを確認すると、異なるプロジェクト（`market-watcher-bot`）にジョブが作成されていた。

### 原因

- 過去に別のプロジェクト（S&P500版）を作成していた
- プロジェクトの切り替えを忘れたまま作業を継続
- `gcloud config set project` でプロジェクトを明示的に設定していなかった

### 解決方法

#### 現在のプロジェクトを確認

```powershell
gcloud config get-value project
```

#### 正しいプロジェクトに切り替え

```powershell
gcloud config set project fx-mxn-watcher-bot
```

#### 間違ったプロジェクトのリソースを削除

```powershell
# 古いプロジェクトに切り替え
gcloud config set project market-watcher-bot

# 不要なジョブを削除
gcloud scheduler jobs delete fx-mxn-watcher-schedule --location=asia-northeast1

# 正しいプロジェクトに戻す
gcloud config set project fx-mxn-watcher-bot
```

### 予防策

- 作業開始時に必ずプロジェクトを確認・設定する
- PowerShellのプロンプトにプロジェクト名を表示させる（PS1設定）
- 複数プロジェクトを扱う場合は、プロジェクト名を明確に区別する命名規則を使う
- 各コマンドに `--project` フラグを明示的に指定する習慣をつける

---

## よくある質問 (FAQ)

### Q1: ログが文字化けする

**A**: PowerShellのエンコーディング設定を確認してください：

```powershell
[Console]::OutputEncoding = [System.Text.Encoding]::UTF8
```

### Q2: デプロイが遅い

**A**: 初回デプロイはDockerイメージのビルドとアップロードで時間がかかります（5〜10分）。2回目以降はキャッシュが効くため高速化されます。

### Q3: Cloud Schedulerが実行されない

**A**: 以下を確認してください：
1. ジョブが `ENABLED` 状態か
2. スケジュール時刻が正しいか（タイムゾーン注意）
3. サービスアカウントに `run.invoker` 権限があるか
4. Cloud Run Jobが存在しているか

### Q4: APIキーが漏洩した可能性がある

**A**: 直ちに以下を実施：
1. OpenAIのダッシュボードで該当キーを無効化
2. 新しいAPIキーを発行
3. Cloud Run Jobsの環境変数を更新
4. GitHubやドキュメントにキーが含まれていないか確認

---

## 参考リンク

- [Google Cloud Run Jobs ドキュメント](https://cloud.google.com/run/docs/create-jobs)
- [Cloud Scheduler ドキュメント](https://cloud.google.com/scheduler/docs)
- [yfinance GitHub](https://github.com/ranaroussi/yfinance)
- [OpenAI API ドキュメント](https://platform.openai.com/docs)

---

## 問題が解決しない場合

このドキュメントに記載のない問題が発生した場合は、以下の手順で情報を収集してください：

1. **エラーログの取得**
   ```powershell
   gcloud logging read "resource.type=cloud_run_job AND resource.labels.job_name=fx-mxn-watcher" `
     --limit 50 `
     --format "table(timestamp, severity, textPayload)"
   ```

2. **リソースの状態確認**
   ```powershell
   gcloud run jobs describe fx-mxn-watcher --region=asia-northeast1
   gcloud scheduler jobs describe fx-mxn-watcher-schedule --location=asia-northeast1
   ```

3. **IAM権限の確認**
   ```powershell
   gcloud projects get-iam-policy fx-mxn-watcher-bot
   ```

4. **進捗メモの更新**
   問題と試した解決方法を `進捗_次回作業メモ.md` に記録しておく
